/**
 * create a list item with an image bullet
 */
image-li(src) {
	content: '';
	display: inline-block;
	height: 32px;
	width: 32px;
	background-size: 32px 32px;
	background-repeat: no-repeat;
	background-image: url(src);
	margin-right: 4px;
}
